{
  "name": "dot-ai-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Display Claude Code / Codex session status and AI usage on a Dot e-ink device",
  "scripts": {
    "test:mix": "node dot_notify.js --test mix",
    "test:hook-fallback": "node dot_notify.js --test hook-fallback",
    "test:usage": "node dot_notify.js --test usage",
    "refresh": "node dot_notify.js --test refresh",
    "usage": "node dot_notify.js --usage"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "optionalDependencies": {
    "canvas": "^3.2.2"
  }
}
